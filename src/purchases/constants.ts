/** RevenueCat entitlement identifier — create matching entitlement in RevenueCat dashboard. */
export const REVENUECAT_ENTITLEMENT_PRO = 'pro';

/** App Store product id for monthly sub — must match ASC + RevenueCat product record (fallback when package type isn’t MONTHLY). */
export const REVENUECAT_MONTHLY_STORE_PRODUCT_ID_IOS = 'a1';

/** Free tier: max successful AI coach replies per local calendar day (resets at midnight). */
export const FREE_COACH_MESSAGES_PER_DAY = 5;
